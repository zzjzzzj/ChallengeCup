# 数据与发布安全

## 仓库允许内容

- 源代码、测试和必要的非数据配置；
- README、运行说明和实验方法文档；
- 不含路径、文件名或样本预测的汇总统计；
- 脱敏后的总体训练指标。

## 禁止提交内容

- 原始或增广图像、YOLO标签和样本清单；
- 能关联到单个样本的文件名、路径、预测明细或哈希；
- 模型权重、ONNX/OM模型、训练日志和缓存；
- 赛事PDF、Word附件或其他未确认公开权限的材料；
- API密钥、账号、令牌和本机绝对路径。

## 提交前检查

```powershell
git status --short
git ls-files
python -m unittest discover -v
```

如果误提交敏感文件，仅在新提交中删除并不能清除历史，需要重写Git历史并更换已经泄露的凭据。
